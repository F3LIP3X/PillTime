export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 44,
} as const;

/** Tamaño táctil mínimo para botones/íconos interactivos (guía Apple/Google). */
export const MIN_TOUCH_TARGET = 44;

/** Alto estándar de un control (botón, campo, fila de lista). */
export const ALTO_CONTROL = 50;
